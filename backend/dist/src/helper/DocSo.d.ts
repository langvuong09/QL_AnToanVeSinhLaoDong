declare class DocSo {
    static doc1so(so: any): string;
    static doc2so(so: any): string;
    static doc3so(so: any): string;
    static docsonguyen(so: any): string;
    static doc(so: any): string;
}
export default DocSo;
